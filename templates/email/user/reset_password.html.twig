{% extends "email/emailBase.html.twig" %}

{% block content %}
    <row class="welcome">
        <columns>
            <spacer size="35"></spacer>
            <h1>
                <center>
                    Bonjour {{ email.toName }}!
                </center>
            </h1>
            <spacer size="10"></spacer>
        </columns>
    </row>
    <spacer size="30"></spacer>
    <row>
        <columns>
            <p>
                Vous avez demandé à réinitialiser votre mot de passe. Pour cela, merci de cliquer sur le lien
                ci-dessous. Celui-ci expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}
            </p>
        </columns>
    </row>
    <row>
        <columns>
            <center>
                <button href="{{ url('app_reset_password', {token: resetToken.token}) }}">Réinitialiser mon mot de passe</button>
            </center>
        </columns>
    </row>
{% endblock %}

