framework:
    rate_limiter:
        account_creation:
            policy: 'fixed_window'
            limit: 1
            interval: '15 minutes'

        resend_confirmation_email:
            policy: 'fixed_window'
            limit: 1
            interval: '5 minutes'
